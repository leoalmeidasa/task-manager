<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Task Manager - Welcome</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
      /* Background Image with Overlay */
      body {
          background-image: url('https://images.unsplash.com/photo-1587614382346-ac7f1c9bc3c9?auto=format&fit=crop&w=1600&q=80');
          background-size: cover;
          background-position: center;
          background-attachment: fixed;
          background-repeat: no-repeat;
      }
  </style>
</head>
<body class="font-sans text-gray-800 min-h-screen flex flex-col">
<!-- Navigation Bar -->
<nav class="bg-blue-600 text-white p-4 shadow-lg">
  <div class="container mx-auto flex justify-between items-center">
    <a href="/" class="navbar-brand fs-3 fw-bold text-white text-decoration-none">
      <i class="bi bi-list-task me-2"></i> Task Manager
    </a>
    <div class="space-x-4">
      <% if user_signed_in? %>
        <span class="text-sm font-medium">Welcome, <%= current_user.email %></span>
        <%= link_to "Sign out", destroy_user_session_path, data: { turbo_method: :delete, turbo: false }, class: "btn btn-danger px-4 py-2 fw-semibold shadow-sm" %>
      <% else %>
        <%= link_to "Sign in", new_user_session_path, class: "btn bg-white text-blue-600 px-5 py-2.5 hover:bg-gray-100" %>
        <%= link_to "Sign up", new_user_registration_path, class: "btn bg-green-500 text-white px-5 py-2.5 hover:bg-green-600" %>
      <% end %>
    </div>
  </div>
</nav>

<!-- Main Content -->
<main class="flex-grow flex items-center justify-center px-4 py-8">
  <div class="w-full max-w-md">
    <!-- Flash Messages -->
    <% if notice %>
      <p class="bg-green-100 text-green-700 p-4 rounded-lg mb-4 shadow-md"><%= notice %></p>
    <% end %>
    <% if alert %>
      <p class="bg-red-100 text-red-700 p-4 rounded-lg mb-4 shadow-md"><%= alert %></p>
    <% end %>

    <!-- Welcome Section (Hidden on Devise Pages) -->
    <% if controller_name == "welcome" || action_name == "index" %>
      <%= yield %>
    <% else %>
      <%= yield %>
    <% end %>
  </div>
</main>

<!-- Footer -->
<footer class="bg-gray-800 text-white text-center p-4">
  <p>© <%= Time.now.year %> Task Manager. All rights reserved.</p>
</footer>
</body>
</html>