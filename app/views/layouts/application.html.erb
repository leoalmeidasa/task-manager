<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Task Manager - Welcome</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
      /* Background Image with Overlay */
      body {
          background-image: url('https://images.unsplash.com/photo-1587614382346-ac7f1c9bc3c9?auto=format&fit=crop&w=1600&q=80');
          background-size: cover;
          background-position: center;
          background-attachment: fixed;
          background-repeat: no-repeat;
      }
  </style>
</head>
<body class="font-sans text-gray-800 min-h-screen flex flex-col">
<!-- Navigation Bar -->
<nav class="bg-blue-600 text-white p-4 shadow-lg">
  <div class="container mx-auto flex justify-between items-center">
    <!-- Brand -->
    <a href="/" class="text-2xl font-bold text-white text-decoration-none flex items-center">
      <i class="bi bi-list-task mr-2"></i> Task Manager
    </a>

    <!-- Hamburger Button for Mobile -->
    <button id="navbar-toggle" class="md:hidden text-white focus:outline-none" aria-label="Toggle navigation">
      <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
      </svg>
    </button>

    <!-- Menu Items -->
    <div id="navbar-menu" class="hidden md:flex md:items-center md:space-x-4 w-full md:w-auto mt-4 md:mt-0">
      <% if user_signed_in? %>
        <span class="text-sm font-medium text-white mr-4"><%= current_user.email %></span>
        <%= link_to "Sign out", destroy_user_session_path, data: { turbo_method: :delete, turbo: false }, class: "btn bg-red-500 text-white px-4 py-2 rounded-md hover:bg-red-600 transition duration-200 md:px-4 md:py-2 px-3 py-1.5 text-sm" %>
      <% else %>
        <%= link_to "Sign in", new_user_session_path, class: "btn bg-white text-blue-600 rounded-md hover:bg-gray-100 transition duration-200 md:px-4 md:py-2 px-3 py-1.5 text-sm mr-2" %>
        <%= link_to "Sign up", new_user_registration_path, class: "btn bg-green-500 text-white rounded-md hover:bg-green-600 transition duration-200 md:px-4 md:py-2 px-3 py-1.5 text-sm" %>
      <% end %>
    </div>
  </div>
</nav>

<!-- Main Content -->
<main class="flex-grow flex items-center justify-center px-4 py-8">
  <div class="w-full max-w-md">
    <!-- Flash Messages -->
    <% if notice %>
      <p class="bg-green-100 text-green-700 p-4 rounded-lg mb-4 shadow-md"><%= notice %></p>
    <% end %>
    <% if alert %>
      <p class="bg-red-100 text-red-700 p-4 rounded-lg mb-4 shadow-md"><%= alert %></p>
    <% end %>

    <!-- Welcome Section (Hidden on Devise Pages) -->
    <% if controller_name == "welcome" || action_name == "index" %>
      <%= yield %>
    <% else %>
      <%= yield %>
    <% end %>
  </div>
</main>

<!-- Footer -->
<footer class="bg-gray-800 text-white text-center p-4">
  <p>© <%= Time.now.year %> Task Manager. All rights reserved.</p>
</footer>
</body>
</html>
<script>
    document.getElementById('navbar-toggle').addEventListener('click', () => {
        const menu = document.getElementById('navbar-menu');
        menu.classList.toggle('hidden');
        menu.classList.toggle('flex');
        menu.classList.toggle('flex-col');
    });
</script>