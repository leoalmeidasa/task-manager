<head>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Bootstrap JS and Popper.js -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <style>
      /* Custom styles for fixed sidebar and responsive content */
      html, body {
          height: 100%;
          margin: 0;
          overflow-x: hidden;
      }
      #sidebar-project {
          min-width: 200px;
          max-width: 250px;
          height: calc(100vh - 55px); /* Adjust 56px to match your navbar height */
          position: fixed;
          top: 80px; /* Offset to start below navbar */
          left: 0;
          z-index: 1000;
          background-color: #1e1f22;
      }
      #content {
          margin-left: 20px;
          padding: 20px;
      }
      /* Mobile screens */
      @media (max-width: 768px) {
          #sidebar-project {
              min-width: 200px; /* Slightly narrower on mobile for better usability */
              max-width: 200px;
          }
          #content {
              margin-left: 200px;
              width: calc(100% - 20px);
          }
      }
      /* Responsive container */
      .container {
          max-width: 100%;
          padding-left: 15px;
          padding-right: 15px;
      }
  </style>
</head>
<body>
<!-- Sidebar -->
<nav id="sidebar-project" class="text-white">
  <div class="p-4">
    <h4 class="text-white">Menu</h4>
    <ul class="nav flex-column">
      <li class="nav-item">
        <a class="nav-link text-white" href="<%= projects_path %>">🏠 Projetos</a>
      </li>
      <li class="nav-item">
        <a class="nav-link text-white" href="<%= new_project_path %>">➕ Novo Projeto</a>
      </li>
      <li class="nav-item">
        <a class="nav-link text-white" href="#">📊 Relatórios</a>
      </li>
      <li class="nav-item">
        <a class="nav-link text-white" href="#">⚙️ Configurações</a>
      </li>
    </ul>
  </div>
</nav>
</body>
<div class="container mt-5">
  <div class="text-center mb-4">
    <h2 class="fw-bold text-secondary">
      <%= @project.persisted? ? 'Editar Projeto' : 'Novo Projeto' %>
    </h2>
  </div>

  <%= form_with(model: @project, local: true, html: { class: "shadow p-4 rounded bg-light" }) do |form| %>

    <% if @project.errors.any? %>
      <div class="alert alert-danger">
        <h5><%= pluralize(@project.errors.count, "erro") %> encontrado(s):</h5>
        <ul class="mb-0">
          <% @project.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="mb-3">
      <%= form.label :name, "Nome do Projeto", class: "form-label fw-semibold" %>
      <%= form.text_field :name, class: "form-control", placeholder: "Digite o nome do projeto" %>
    </div>

    <div class="mb-3">
      <%= form.label :description, "Descrição", class: "form-label fw-semibold" %>
      <%= form.text_area :description, class: "form-control", placeholder: "Descreva brevemente o projeto", rows: 4 %>
    </div>

    <div class="text-end">
      <%= form.submit (@project.persisted? ? 'Atualizar' : 'Criar Projeto'), class: "btn btn-primary px-4" %>
    </div>

  <% end %>
</div>
