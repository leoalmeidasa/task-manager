<head>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Bootstrap JS and Popper.js -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <style>
      /* Custom styles for fixed sidebar and responsive content */
      html, body {
          height: 100%;
          margin: 0;
          overflow-x: hidden;
      }
      #sidebar-project {
          min-width: 200px;
          max-width: 250px;
          height: calc(100vh - 55px); /* Adjust 56px to match your navbar height */
          position: fixed;
          top: 80px; /* Offset to start below navbar */
          left: 0;
          z-index: 1000;
          background-color: #1e1f22;
      }
      #content {
          margin-left: 20px;
          padding: 20px;
      }
      /* Mobile screens */
      @media (max-width: 768px) {
          #sidebar-project {
              min-width: 200px; /* Slightly narrower on mobile for better usability */
              max-width: 200px;
          }
          #content {
              margin-left: 200px;
              width: calc(100% - 20px);
          }
      }
      /* Responsive container */
      .container {
          max-width: 100%;
          padding-left: 15px;
          padding-right: 15px;
      }
  </style>
</head>
<body>
<!-- Sidebar -->
<nav id="sidebar-project" class="text-white">
  <div class="p-4">
    <h4 class="text-white">Menu</h4>
    <ul class="nav flex-column">
      <li class="nav-item">
        <a class="nav-link text-white" href="<%= projects_path %>">🏠 Projetos</a>
      </li>
      <li class="nav-item">
        <a class="nav-link text-white" href="<%= new_project_path %>">➕ Novo Projeto</a>
      </li>
      <li class="nav-item">
        <a class="nav-link text-white" href="#">📊 Relatórios</a>
      </li>
      <li class="nav-item">
        <a class="nav-link text-white" href="#">⚙️ Configurações</a>
      </li>
    </ul>
  </div>
</nav>
</body>
<%= content_tag :div, class: "container mt-5" do %>
  <%= content_tag :div, class: "card shadow-sm" do %>
    <%= content_tag :div, class: "card-body" do %>
      <%= content_tag :h1, (@project.name.present? ? @project.name : t('projects.untitled')), class: "card-title mb-4" %>

      <%= content_tag :div, class: "mb-4" do %>
        <%= content_tag :p, class: "card-text" do %>
          <%= content_tag :strong, t('projects.description') %>:
          <%= @project.description.present? ? simple_format(@project.description) : t('projects.no_description') %>
        <% end %>

        <%= content_tag :p, class: "card-text" do %>
          <%= content_tag :strong, t('projects.created_by') %>:
          <%= @project.user&.email || t('projects.unknown_user') %>
        <% end %>
      <% end %>

          <%= content_tag :div, class: "d-flex flex-wrap gap-2" do %>
            <%= link_to t('actions.edit'), edit_project_path(@project),
                        class: "btn btn-primary",
                        data: { turbo: false } %>

            <%= link_to t('actions.back'), projects_path,
                        class: "btn btn-outline-secondary" %>

            <%= link_to t('projects.manage_members'), project_project_members_path(@project),
                         class: "btn btn-outline-secondary" %>

            <%= link_to t('projects.view_tasks'), project_tasks_path(@project),
                        class: "btn btn-outline-secondary" %>
          <% end %>
        <% end %>
      <% end %>
    <% end %>

    <% content_for :head do %>
      <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
      <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    <% end %>